{
  "name": "denuncias-publicas",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "lint": "pnpm -r run lint"
  }
}
